from __future__ import annotations

"""Hidden knowledge discovery: dependency graph, module scoring, structural
signals, git evidence, and candidate/question generation.

v1 is offline and rule-based: it never calls an LLM and never writes into the
knowledge registry. Outputs are review artifacts (reports) only.
"""
